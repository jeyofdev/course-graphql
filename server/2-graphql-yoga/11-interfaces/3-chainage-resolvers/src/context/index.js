import characters from '../../datas/characters.json';
import wands from '../../datas/wands.json';

const context = {
    datas: {
        characters,
        wands,
    },
};

export default context;
